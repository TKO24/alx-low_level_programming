#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - A function that print if a number is positive, negative or zero
 * Return: Always (Success)
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	putchar(i);
	putchar('\n');

	return (0);
}
