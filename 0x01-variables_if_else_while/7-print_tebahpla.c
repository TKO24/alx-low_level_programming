#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - A function that print if a number is positive, negative or zero
 * Return: Always (Success)
 */
int main(void)
{
char ch;

for (ch = 'z'; ch >= 'a'; ch--)
{
putchar(ch);
}
putchar('\n');
return (0);
}
