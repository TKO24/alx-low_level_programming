#include "main.h"

/**
 * _puts - a function that prints a string, followed by a new line, to stdout.
 * @str: input
 */

void print_rev(char *s)
{
	while (*str != '\0')
	{
		_putchar(*str++);
	}
	_putchar('\n');
}
